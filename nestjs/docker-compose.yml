services:
  redis:
    image: redis:7.4-alpine
    container_name: hmac-lab-nest-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    volumes:
      - redis_data_nest:/data
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ./redis/users.acl:/usr/local/etc/redis/users.acl:ro
    healthcheck:
      test: ["CMD", "redis-cli", "--user", "user", "-a", "password", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  api_1:
    image: node:22-alpine
    container_name: hmac-lab-nest-api-1
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      PORT: "3001"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-nest-api1"
      HMAC_CLIENT_ID: "clientIdAbC"
      HMAC_BOOTSTRAP_SECRET: "superSharedSecret"
      HMAC_SECRET_TOKEN: "sharedHmacToken"
      PEER_BASE_URL: "http://api_2:3002"
    ports:
      - "3001:3001"
    volumes:
      - ./api_1:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_2:
    image: node:22-alpine
    container_name: hmac-lab-nest-api-2
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      PORT: "3002"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-nest-api2"
      HMAC_CLIENT_ID: "clientIdAbC"
      HMAC_BOOTSTRAP_SECRET: "superSharedSecret"
      HMAC_SECRET_TOKEN: "sharedHmacToken"
      PEER_BASE_URL: "http://api_1:3001"
    ports:
      - "3002:3002"
    volumes:
      - ./api_2:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_shared_1:
    image: node:22-alpine
    container_name: hmac-lab-nest-shared-api-1
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      API_NAME: "api_shared_1"
      PORT: "3021"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-shared-nest-1"
      HMAC_SECRET_TOKEN: "sharedInternalToken"
      INTERNAL_MANAGEMENT_ROUTE: "/api/internal/hmac"
      DEFAULT_SHARED_CLIENT_ID: "sharedClientAbC"
      DEFAULT_SHARED_CLIENT_SECRET: "superSharedSecret"
      PEER_APIS: "http://api_shared_2:3022,http://api_shared_3:3023,http://api_shared_4:3024,http://api_shared_5:3025"
    ports:
      - "3021:3021"
    volumes:
      - ./api_shared_1:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_shared_2:
    image: node:22-alpine
    container_name: hmac-lab-nest-shared-api-2
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      API_NAME: "api_shared_2"
      PORT: "3022"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-shared-nest-2"
      HMAC_SECRET_TOKEN: "sharedInternalToken"
      INTERNAL_MANAGEMENT_ROUTE: "/api/internal/hmac"
      DEFAULT_SHARED_CLIENT_ID: "sharedClientAbC"
      DEFAULT_SHARED_CLIENT_SECRET: "superSharedSecret"
      PEER_APIS: "http://api_shared_1:3021,http://api_shared_3:3023,http://api_shared_4:3024,http://api_shared_5:3025"
    ports:
      - "3022:3022"
    volumes:
      - ./api_shared_2:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_shared_3:
    image: node:22-alpine
    container_name: hmac-lab-nest-shared-api-3
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      API_NAME: "api_shared_3"
      PORT: "3023"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-shared-nest-3"
      HMAC_SECRET_TOKEN: "sharedInternalToken"
      INTERNAL_MANAGEMENT_ROUTE: "/api/internal/hmac"
      DEFAULT_SHARED_CLIENT_ID: "sharedClientAbC"
      DEFAULT_SHARED_CLIENT_SECRET: "superSharedSecret"
      PEER_APIS: "http://api_shared_1:3021,http://api_shared_2:3022,http://api_shared_4:3024,http://api_shared_5:3025"
    ports:
      - "3023:3023"
    volumes:
      - ./api_shared_3:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_shared_4:
    image: node:22-alpine
    container_name: hmac-lab-nest-shared-api-4
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      API_NAME: "api_shared_4"
      PORT: "3024"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-shared-nest-4"
      HMAC_SECRET_TOKEN: "sharedInternalToken"
      INTERNAL_MANAGEMENT_ROUTE: "/api/internal/hmac"
      DEFAULT_SHARED_CLIENT_ID: "sharedClientAbC"
      DEFAULT_SHARED_CLIENT_SECRET: "superSharedSecret"
      PEER_APIS: "http://api_shared_1:3021,http://api_shared_2:3022,http://api_shared_3:3023,http://api_shared_5:3025"
    ports:
      - "3024:3024"
    volumes:
      - ./api_shared_4:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

  api_shared_5:
    image: node:22-alpine
    container_name: hmac-lab-nest-shared-api-5
    working_dir: /app
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      API_NAME: "api_shared_5"
      PORT: "3025"
      REDIS_URL: "redis://user:password@redis:6379"
      HMAC_NAMESPACE: "hmac-lab-shared-nest-5"
      HMAC_SECRET_TOKEN: "sharedInternalToken"
      INTERNAL_MANAGEMENT_ROUTE: "/api/internal/hmac"
      DEFAULT_SHARED_CLIENT_ID: "sharedClientAbC"
      DEFAULT_SHARED_CLIENT_SECRET: "superSharedSecret"
      PEER_APIS: "http://api_shared_1:3021,http://api_shared_2:3022,http://api_shared_3:3023,http://api_shared_4:3024"
    ports:
      - "3025:3025"
    volumes:
      - ./api_shared_5:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev:nodemon"

volumes:
  redis_data_nest:
